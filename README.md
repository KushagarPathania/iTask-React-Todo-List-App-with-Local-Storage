# iTask-React-Todo-List-App-with-Local-Storage
iTask is a simple and efficient todo list application built using React. It allows users to add, edit, delete, and mark tasks as completed. Todos are stored in the browser’s localStorage, ensuring data persistence even after page refresh. The app features a clean UI and an option to toggle completed tasks for better task management.
